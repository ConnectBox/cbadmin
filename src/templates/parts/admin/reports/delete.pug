extends ../../../layout/form
mixin script(name, id)
    div.mt-3
        include  ../../../icons/settings
        = name
        .float-end
            button.bg-transparent.border-0.text-primary.p-0(id=`${id}-button`)
                include  ../../../icons/play

block icon
    include ../../../icons/chart

block title
    | Delete Stats

block content
    +script('Delete Stats', 'deleteweblogs')

